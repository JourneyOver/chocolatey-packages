{
  "name": "chocolatey-packages",
  "version": "1.0.0",
  "description": "Hooks and other things for allowing development",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/JourneyOver/chocolatey-packages.git"
  },
  "bugs": {
    "url": "https://github.com/JourneyOver/chocolatey-packages/issues"
  },
  "homepage": "https://github.com/JourneyOver/chocolatey-packages#readme",
  "dependencies": {
    "imagemin-lint-staged": "^0.4.0",
    "lint-staged": "^10.3.0"
  },
  "lint-staged": {
    "*.{md,json,yml,yaml}": [
      "prettier --write",
      "git add"
    ],
    "*.{jpeg,jpg,gif,png,svg}": [
      "imagemin-lint-staged",
      "git add"
    ]
  }
}
