# Roadmap

## Automatic Packages

`~` origin

`~` taiga

`~` kcp black

`~` uplay

`~` gog galaxy

`~` cleanmem

`~` gameplay time tracker

`~` adobe air

`~` rivatuner statistics server

`~` game jolt

`~` unity web player

`~` steam

`~` infinity

`~` BleachBit

## Cinst-gh packages

`~` cleanmem

`~` firehawk

## Manual Packages

# Done

`+` Process Lasso

`+` simple static IP

`+` winpatrol
